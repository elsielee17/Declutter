# include packages
library(tidyr)
library(dplyr)
library(heplots)
library(MANOVA.RM)
library(multcomp)
library(lme4)
library(stringr)
library(ggplot2)
library(car)
library(sjstats)
# package for Chi-Square
library(MASS)
redraw <- read.csv("exp1_redraw_clean.csv")
model <- lmer(score ~ design*topic + (1|p), data = redraw)
Anova(model)
anova_stats(model)
summary(glht(model , linfct=mcp(design = "Tukey")))
model <- lmer(score ~ design*topic + (1|p), data = redraw)
Anova(model)
anova_stats(model)
summary(glht(model , linfct=mcp(design = "Tukey")))
summary(glht(model , linfct=mcp(topic = "Tukey")))
5.852e-01 *10
redraw$designTopic <- paste(redraw$design, redraw$topic)
model <- lmer(score ~ designTopic + (1|p), data = redraw)
Anova(model)
anova_stats(model)
summary(glht(model , linfct=mcp(designTopic = "Tukey")))
